Algoritmo "tabuada"

Var

   x, i, conta : inteiro

Inicio

   escreva("Deseja a tabuada para qual valor? ")
   leia(x)
   
   para i de 1 ate 10 faca
      conta <- x * i
      escreval(x, " x", i, " =", conta)
   fimpara

Fimalgoritmo